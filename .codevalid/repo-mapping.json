{
  "addition": {
    "implementation": [
      {
        "id": "cal_py_addition_branch",
        "name": "Addition branch in main loop",
        "file_path": "cal.py",
        "type": "function",
        "How_is_it_connected_to_implementation": "Implements the addition operation when the user selects option 1. It reads two numbers (which can be negative), adds them, and prints the result.",
        "pseudo_code": "LOOP forever:\n    Display calculator menu\n    Get user choice as integer\n    Get first number as integer input\n    Get second number as integer input\n    IF choice == 1:\n        Output the sum of first and second number\n    ELSE IF choice == 2:\n        Output the difference\n    ELSE IF choice == 3:\n        Output the product\n    ELSE IF choice == 4:\n        Output the quotient\n    ELSE IF choice == 5:\n        Exit loop\n    Print a blank line"
      }
    ],
    "missing_implementations": []
  },
  "subtraction": {
    "implementation": [],
    "missing_implementations": [
      "Given two numbers, the greater number should be subtracted by the smaller one.",
      "Output can be negative or positive.",
      "Input numbers can be negative or positive."
    ]
  },
  "multiplication": {
    "implementation": [
      {
        "id": "cal_py_main_loop",
        "name": "main calculator loop",
        "file_path": "cal.py",
        "type": "function",
        "How_is_it_connected_to_implementation": "Implements a menu-driven calculator. When the user selects option 3 (Multiplication), it multiplies two input numbers and prints the output.",
        "pseudo_code": "Print welcome message\nLoop forever:\n    Print menu options\n    Get user choice\n    Get first number\n    Get second number\n    IF choice == 1:\n        Print sum\n    ELSE IF choice == 2:\n        Print difference\n    ELSE IF choice == 3:\n        Print product (num1 * num2)\n    ELSE IF choice == 4:\n        Print division\n    ELSE IF choice == 5:\n        Print exit message\n        Break loop\n    Print newline"
      }
    ],
    "missing_implementations": [
      "Output should not be decimal"
    ]
  },
  "division": {
    "implementation": [
      {
        "id": "calculator_division_branch_cal.py",
        "name": "Division branch in calculator main loop",
        "file_path": "cal.py",
        "type": "function",
        "How_is_it_connected_to_implementation": "Implements division operation when user selects option 4. Directly performs division using num1 / num2 and prints the result.",
        "pseudo_code": "IF user selects option 4 (Division):\n    Prompt for first number (num1)\n    Prompt for second number (num2)\n    Output: num1 / num2"
      }
    ],
    "missing_implementations": [
      "Should not be divided by zero.",
      "Always handle the exception of dividing by zero."
    ]
  }
}